# Configuration worksheet, not a runnable release.
# Required values are intentionally empty until a tested MCP image exists.
MCP_IMAGE=
CADDY_IMAGE=
MCP_DOMAIN=
MCP_AUTH_AUDIENCE=
INVENTREE_URL=
MCP_AUTH_BROKER_CONFIG_FILE=
# Keep loopback until public HTTPS and end-to-end authorization are verified.
MCP_BIND_ADDRESS=127.0.0.1
