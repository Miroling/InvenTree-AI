Deployment scaffolding rendered for {{ include "inventree-ai.name" . }}.

This repository does not yet publish an MCP runtime image. Rendering is not
evidence that the supplied image implements authentication or InvenTree tools.
See docs/deployment/runtime-contract.md and docs/deployment/README.md.

Public access requires a tested authenticated runtime, authorized users,
an OAuth provider, and valid TLS. Discovery metadata is public; MCP calls are not.
Secret rotation requires a rollout until runtime reload behavior is implemented.
